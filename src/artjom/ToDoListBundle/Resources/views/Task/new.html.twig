{% extends '::base.html.twig' %}

{% block title %}New task{% endblock %}

{% block body -%}
    <h2>New task</h2>
	
	<div class="row">
		<div class="col-xs-6 col-sm-4">
	
			{{ form_start(form, {'attr': {'role': 'form'}}) }}
	
			<div class="form-group">
				{{ form_label(form.title) }}
				{{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
				{% if form.title.vars.errors|length %}
					<div class="alert alert-danger">
						{{ form_errors(form.title) }}
					</div>
				{% endif %}
			</div>
			
			<div class="form-group">
				{{ form_label(form.status) }}
				{{ form_widget(form.status, {'attr': {'class': 'form-control'}}) }}
				{% if form.status.vars.errors|length %}
					<div class="alert alert-danger">
						{{ form_errors(form.status) }}
					</div>
				{% endif %}
			</div>
			
			<div class="form-group">
				{{ form_label(form.start_date) }}
				{{ form_widget(form.start_date, {'attr': {'class': 'form-control'}}) }}
				{% if form.start_date.vars.errors|length %}
					<div class="alert alert-danger">
						{{ form_errors(form.start_date) }}
					</div>
				{% endif %}
			</div>
			
			<div class="form-group">
				{{ form_label(form.end_date) }}
				{{ form_widget(form.end_date, {'attr': {'class': 'form-control'}}) }}
				{% if form.end_date.vars.errors|length %}
					<div class="alert alert-danger">
						{{ form_errors(form.end_date) }}
					</div>
				{% endif %}
			</div>
			
			<div class="form-group">
				{{ form_label(form.description) }}
				{{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
				{% if form.description.vars.errors|length %}
					<div class="alert alert-danger">
						{{ form_errors(form.description) }}
					</div>
				{% endif %}
			</div>
			
			{{ form_widget(form.submit, {'attr': {'class': 'btn btn-default btn-lg active', 'formnovalidate': 'formnovalidate'}}) }}
			
			<a href="{{ path('task_list') }}" role="button" class="btn btn-primary btn-lg active">
				Cancel
			</a>
	
			{{ form_end(form) }}
	
		</div>
	</div>
{% endblock %}
